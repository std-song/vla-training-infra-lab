diff --git a/vlash/configs/train_config.py b/vlash/configs/train_config.py
index 4ff04d3..4b4fcb6 100644
--- a/vlash/configs/train_config.py
+++ b/vlash/configs/train_config.py
@@ -106,5 +106,9 @@ class VLASHTrainConfig(TrainPipelineConfig):
     # branches d>0 use predicted s(t+d) instead of endpoint action a(t+d-1).
     future_state_predictor_path: str | None = None

+    # State conditioning for delayed branches: action_proxy (upstream VLASH),
+    # stale, learned, oracle, or auto (learned when a predictor path is set).
+    future_state_mode: str = "auto"
+
     # LoRA configuration
     lora: LoRAConfig = field(default_factory=LoRAConfig)
diff --git a/vlash/datasets/libero_state_predictor.py b/vlash/datasets/libero_state_predictor.py
new file mode 100644
index 0000000..cec58e7
--- /dev/null
+++ b/vlash/datasets/libero_state_predictor.py
@@ -0,0 +1,67 @@
+"""Sequence-conditioned future-state model for LIBERO's mismatched state/action spaces."""
+
+from __future__ import annotations
+
+import torch
+from torch import nn
+
+
+class ActionSequenceStatePredictor(nn.Module):
+    """Predict normalized future state from current state and a planned action prefix."""
+
+    def __init__(self, state_dim: int, action_dim: int, max_horizon: int, hidden_dim: int = 256):
+        super().__init__()
+        self.state_dim = state_dim
+        self.action_dim = action_dim
+        self.max_horizon = max_horizon
+        self.state_encoder = nn.Sequential(
+            nn.Linear(state_dim, hidden_dim),
+            nn.GELU(),
+            nn.Linear(hidden_dim, hidden_dim),
+        )
+        self.action_encoder = nn.Linear(action_dim, hidden_dim)
+        self.step_embedding = nn.Embedding(max_horizon, hidden_dim)
+        self.action_gru = nn.GRU(hidden_dim, hidden_dim, batch_first=True)
+        self.state_decoder = nn.Sequential(
+            nn.Linear(hidden_dim + state_dim + 1, hidden_dim),
+            nn.GELU(),
+            nn.Linear(hidden_dim, state_dim),
+        )
+
+    def forward(
+        self,
+        current_state_normalized: torch.Tensor,
+        action_prefix_normalized: torch.Tensor,
+        horizon: torch.Tensor,
+    ) -> torch.Tensor:
+        """Return predicted future state in normalized state coordinates."""
+        batch_size, sequence_length, _ = action_prefix_normalized.shape
+        if sequence_length != self.max_horizon:
+            raise ValueError(f"Expected {self.max_horizon} actions, got {sequence_length}")
+        steps = torch.arange(self.max_horizon, device=horizon.device)
+        action_tokens = self.action_encoder(action_prefix_normalized)
+        action_tokens = action_tokens + self.step_embedding(steps).unsqueeze(0)
+        initial_hidden = self.state_encoder(current_state_normalized).unsqueeze(0)
+        outputs, _ = self.action_gru(action_tokens, initial_hidden)
+        selected = outputs[torch.arange(batch_size, device=horizon.device), horizon - 1]
+        horizon_feature = horizon.unsqueeze(1).to(current_state_normalized.dtype) / self.max_horizon
+        delta = self.state_decoder(torch.cat((selected, current_state_normalized, horizon_feature), dim=1))
+        return current_state_normalized + delta
+
+
+def predict_future_state(
+    model: ActionSequenceStatePredictor,
+    current_state: torch.Tensor,
+    action_prefix: torch.Tensor,
+    horizon: torch.Tensor,
+    stats: dict[str, torch.Tensor],
+) -> torch.Tensor:
+    state_mean = stats["state_mean"].to(current_state)
+    state_std = stats["state_std"].to(current_state)
+    action_mean = stats["action_mean"].to(action_prefix)
+    action_std = stats["action_std"].to(action_prefix)
+    state_normalized = (current_state - state_mean) / state_std
+    action_normalized = (action_prefix - action_mean) / action_std
+    prediction_normalized = model(state_normalized, action_normalized, horizon)
+    return prediction_normalized * state_std + state_mean
+
diff --git a/vlash/datasets/vlash_dataset.py b/vlash/datasets/vlash_dataset.py
index 5fff9af..f4b0ea4 100644
--- a/vlash/datasets/vlash_dataset.py
+++ b/vlash/datasets/vlash_dataset.py
@@ -55,7 +55,14 @@ from torch.utils.data._utils.collate import default_collate

 from lerobot.datasets.lerobot_dataset import LeRobotDataset

-from .future_state_predictor import ResidualStatePredictor, predict_future_state
+from .future_state_predictor import (
+    ResidualStatePredictor,
+    predict_future_state as predict_residual_future_state,
+)
+from .libero_state_predictor import (
+    ActionSequenceStatePredictor,
+    predict_future_state as predict_sequence_future_state,
+)


 class VLASHDataset(LeRobotDataset):
@@ -87,6 +94,7 @@ class VLASHDataset(LeRobotDataset):
         *,
         use_state_ground_truth: bool = False,
         future_state_predictor_path: str | Path | None = None,
+        future_state_mode: str = "auto",
     ):
         """Initialize VLASH dataset.

@@ -111,6 +119,14 @@ class VLASHDataset(LeRobotDataset):
         self.use_state_ground_truth = use_state_ground_truth
         self.future_state_predictor = None
         self.future_state_predictor_stats = None
+        self.predict_future_state_fn = None
+        if use_state_ground_truth:
+            future_state_mode = "oracle"
+        elif future_state_mode == "auto":
+            future_state_mode = "learned" if future_state_predictor_path is not None else "action_proxy"
+        if future_state_mode not in {"action_proxy", "stale", "learned", "oracle"}:
+            raise ValueError(f"Unsupported future_state_mode: {future_state_mode}")
+        self.future_state_mode = future_state_mode

         super().__init__(
             repo_id=repo_id,
@@ -131,7 +147,13 @@ class VLASHDataset(LeRobotDataset):

         if future_state_predictor_path is not None:
             checkpoint = torch.load(future_state_predictor_path, map_location="cpu", weights_only=False)
-            self.future_state_predictor = ResidualStatePredictor(**checkpoint["config"])
+            predictor_config = checkpoint["config"]
+            if predictor_config["state_dim"] == predictor_config["action_dim"]:
+                self.future_state_predictor = ResidualStatePredictor(**predictor_config)
+                self.predict_future_state_fn = predict_residual_future_state
+            else:
+                self.future_state_predictor = ActionSequenceStatePredictor(**predictor_config)
+                self.predict_future_state_fn = predict_sequence_future_state
             self.future_state_predictor.load_state_dict(checkpoint["model"])
             self.future_state_predictor.eval()
             self.future_state_predictor_stats = checkpoint["stats"]
@@ -140,6 +162,8 @@ class VLASHDataset(LeRobotDataset):
                     "future-state predictor max_horizon is smaller than max_delay_steps: "
                     f"{self.future_state_predictor.max_horizon} < {self.max_delay_steps}"
                 )
+        if self.future_state_mode == "learned" and self.future_state_predictor is None:
+            raise ValueError("future_state_mode=learned requires future_state_predictor_path")

     def _get_query_indices(self, idx: int, ep_idx: int) -> tuple[dict[str, list[int | bool]]]:
         """Get query indices with random temporal offset.
@@ -208,6 +232,11 @@ class VLASHDataset(LeRobotDataset):
         if offset <= 0:
             return item

+        if self.future_state_mode == "stale":
+            return item
+        if self.future_state_mode == "learned":
+            raise ValueError("future_state_mode=learned requires shared_observation=true")
+
         # Get episode boundaries
         ep_idx = item["episode_index"].item() if "episode_index" in item else None
         if ep_idx is None:
@@ -217,7 +246,7 @@ class VLASHDataset(LeRobotDataset):
         ep_start = ep["dataset_from_index"]
         ep_end = ep["dataset_to_index"]

-        if self.use_state_ground_truth:
+        if self.future_state_mode == "oracle":
             # Use recorded future state s_{t+offset}
             future_idx = max(ep_start, min(ep_end - 1, idx + offset))
             new_state = self.hf_dataset[future_idx]["observation.state"]
@@ -279,6 +308,7 @@ class SharedObservationVLASHDataset(VLASHDataset):
         *,
         use_state_ground_truth: bool = False,
         future_state_predictor_path: str | Path | None = None,
+        future_state_mode: str = "auto",
     ):
         """Initialize SharedObservationVLASHDataset.

@@ -299,6 +329,7 @@ class SharedObservationVLASHDataset(VLASHDataset):
             max_delay_steps=max_delay_steps,
             use_state_ground_truth=use_state_ground_truth,
             future_state_predictor_path=future_state_predictor_path,
+            future_state_mode=future_state_mode,
         )

     def _get_query_indices_for_offset(
@@ -385,9 +416,12 @@ class SharedObservationVLASHDataset(VLASHDataset):
             if offset == 0:
                 state = base_item["observation.state"]
             else:
-                if self.use_state_ground_truth:
+                if self.future_state_mode == "oracle":
                     future_idx = max(ep_start, min(ep_end - 1, idx + offset))
                     state = self.hf_dataset[future_idx]["observation.state"]
+                elif self.future_state_mode in {"stale", "learned"}:
+                    # Learned states are filled in after the action prefixes are collected.
+                    state = base_item["observation.state"]
                 else:
                     # State is previous action (action at t + offset - 1)
                     prev_idx = max(ep_start, min(ep_end - 1, idx + offset - 1))
@@ -421,13 +455,13 @@ class SharedObservationVLASHDataset(VLASHDataset):
             action_is_pads.append(padding["action_is_pad"])

         stacked_states = torch.stack(states, dim=0)
-        if self.future_state_predictor is not None and num_offsets > 1:
+        if self.future_state_mode == "learned" and num_offsets > 1:
             horizons = torch.arange(1, num_offsets, dtype=torch.long)
             current_state = base_item["observation.state"].unsqueeze(0).expand(num_offsets - 1, -1)
             current_actions = actions[0][: self.future_state_predictor.max_horizon]
             action_prefix = current_actions.unsqueeze(0).expand(num_offsets - 1, -1, -1)
             with torch.inference_mode():
-                learned_states = predict_future_state(
+                learned_states = self.predict_future_state_fn(
                     self.future_state_predictor,
                     current_state,
                     action_prefix,
diff --git a/vlash/train.py b/vlash/train.py
index 98041ea..a26acd6 100644
--- a/vlash/train.py
+++ b/vlash/train.py
@@ -127,6 +127,7 @@ def make_vlash_dataset(cfg: VLASHTrainConfig):
             video_backend=cfg.dataset.video_backend,
             max_delay_steps=cfg.max_delay_steps,
             future_state_predictor_path=cfg.future_state_predictor_path,
+            future_state_mode=cfg.future_state_mode,
         )
     else:
         # Log the temporal delay configuration
@@ -137,8 +138,8 @@ def make_vlash_dataset(cfg: VLASHTrainConfig):
             )
         else:
             logging.info("Creating VLASHDataset with max_delay_steps=0 (no temporal delay)")
-        if cfg.future_state_predictor_path is not None:
-            raise ValueError("future_state_predictor_path requires shared_observation=true")
+        if cfg.future_state_mode == "learned" or cfg.future_state_predictor_path is not None:
+            raise ValueError("learned future-state conditioning requires shared_observation=true")

         # Create dataset with temporal offset augmentation
         dataset = VLASHDataset(
@@ -150,6 +151,7 @@ def make_vlash_dataset(cfg: VLASHTrainConfig):
             revision=cfg.dataset.revision,
             video_backend=cfg.dataset.video_backend,
             max_delay_steps=cfg.max_delay_steps,
+            future_state_mode=cfg.future_state_mode,
         )

     # Apply ImageNet stats if requested (same as original make_dataset)
